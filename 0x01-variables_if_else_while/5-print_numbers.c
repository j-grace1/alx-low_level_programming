#include <stdio.h>

/**
 * main - Prints the decimal numbers from 0 to 9
 *
 * Return: Always successful
 */

int main(void)
{
	int num;

	for (num = "0"; num <= "0"; num++)
		putchar(num);

	putchar('\n');

	return (0);
}
