#include "main.h"

/**
 * main - alphabet in lowercase
 * 
 * Return: returns 0
 * 
 */
void print_alphabet(void)
    {
    for(char i = 'a'; i <= 'z'; i++)
        {
            _putchar(i);
        }

    _putchar('\n');
    }
